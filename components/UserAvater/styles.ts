import styled from 'styled-components'

export const StyledUserAvater = styled.div`
	display: flex;
	align-items: center;
	.avater {
		height: 40px;
		width: 40px;
		border-radius: 100%;
		margin-right: 1.3rem;
	}

	.info {
		font-family: inherit;
		.name {
			font-size: 1.6rem;
		}
		.username {
			font-size: 1.4rem;
		}
	}
`
